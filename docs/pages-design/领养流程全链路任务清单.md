# PawHome 领养流程全链路任务清单

> 审计日期：2026-09-03  
> 审计范围：领养入口、选猫、申请、申请结果、我的领养、院主审核、确认领养、评审/证实、奖励结果、地址奖励，以及领养额度/助力扩展状态。  
> 当前结论：**本地持久化领养主链路、拒绝分支和放弃分支已通过已打开的微信开发者工具验收**。本轮验收对象是本地演示状态机；生产接口仍未接入，见第 5 节。

## 1. 设计与实现约束

- 设计坐标以 Figma 文件 `03pt0RiVfBFCvn4PsIyezk` 的 375px 基准为准；已用精确节点核对核心画板：`62:31044`、`62:31384`、`62:36699`、`62:35297`、`62:36210`。
- 采用现有 `PawPageNav`、`PawFixedActionBar`、`PawBottomSheet`、`PawDialog` 等共享组件；页面布局优先 flex/流式布局。
- Figma 中的状态栏、微信胶囊、Home Indicator 只作为设计参考，微信端不绘制；不使用整屏/局部界面截图作为页面 UI。可使用 Figma 导出的独立宠物、头像、内容照片素材。
- 不通过业务页面追加 `scale`、`translateY` 等补丁修复结构问题；不改运行时 size 规则。
- 任务全部完成并标记 `DONE` 后，才执行一次完整领养链路测试；中途只允许做构建、静态检查和非链路性的局部验证。

## 2. 当前审计发现

| 编号 | 结论 | 证据 | 影响 |
| --- | --- | --- | --- |
| A-01 | `SOURCE_FIX` | `components/AdoptPickCatsSheet.vue` 在传入 `figma-mode` 时用 `v-if="!figmaMode"` 隐藏所有宠物图片。`pages/adoption/pickCats.vue` 正是以 `figma-mode` 打开。 | 选猫页实际看不到宠物，入口链路无法可靠开始。 |
| A-02 | `SOURCE_FIX` | `components/PawAdoptionFlowFigma.vue` 的审核、确认、放弃、评价按钮统一只调用 `showToast`，没有更新领养记录或跳转到下一状态。 | `44–57` 只能浏览画面，不能完成审核、确认、评审后续链路。 |
| A-03 | `SOURCE_FIX` | `pages/yard/adoptionAudit.vue` 只从 `getDemoAdoptions()` 读取记录；动态申请记录从 `getAdoptions()` 写入后，审核页无法稳定读到并更新。 | 真实填写的申请与院主审核之间断链。 |
| A-04 | `SOURCE_FIX` | `pages/meMore/myAdoption.vue` 把记录转换成 `-view-${index}` 假 ID，并把每条宠物都替换为五只“小灰灰”；详情页收到该 ID 后找不到原记录。 | “申请成功 → 我的领养 → 详情”无法还原同一条数据。 |
| A-05 | `SOURCE_FIX` | `pages/adoption/result.vue` 的所有 variant 共用 `onAction(){ uni.navigateBack() }`，没有按 `80–85` 进入订单、详情或进度。 | 结果页 CTA 不通，分支状态无法继续。 |
| A-06 | `SOURCE_FIX` | `pages/adoption/submitOrder.vue` 未读取/保存收货地址，提交时直接跳转 `result?variant=80`，且没有传递领养记录 ID。 | 奖励地址未完成，订单与领养记录无法关联。 |
| A-07 | `SOURCE_FIX` | `PawAdoptionEvidence` 的确认页上传区域没有 `chooseMedia`，提交只 `showToast`；证实列表的“我也来证实”没有动作。 | 证实/确认链路停在静态表单。 |
| A-08 | `SOURCE_FIX` | 多个领养页面仍自行读取 `statusBarHeight`/`getMenuButtonBoundingClientRect()` 并绘制私有导航；`adoptApply`、`adoptApplySuccess`、`adoptionDetail`、`adoptionApplyContent`、`adoptionAudit`、`myAdoption` 均未复用 `PawPageNav`。 | 违反导航几何单一来源，连续尺寸、不同设备和真机安全区有风险。 |
| A-09 | `SOURCE_FIX` | `PawAdoptionFlowFigma`、`PawAdoptionEvidence`、`adoptApply` 的 H5 分支使用 `top-*.png`/`*-top.png` 作为状态栏或页面顶部参考图；部分页面还绘制胶囊/系统栏。 | 不符合“不允许使用切图”和 native chrome 规则；应改为真实布局与独立素材。 |
| A-10 | `SOURCE_FIX` | 领养状态模型存在 `pending/rejected/pickup/owner_confirm/jury_confirm/reward`，但缺少统一的合法状态转换、当前角色和记录 ID 传递。部分操作直接跳到 `redirectTo` 的演示 mode。 | 刷新、返回、重复操作后状态可能丢失或跳错分支。 |
| A-11 | `SOURCE_FIX` | 领养扩展页 `extras.vue` 使用硬编码额度、列表、弹窗结果；额度不足弹窗没有关闭/回到入口的操作，助力领养链接没有真正跳转。 | 额度限制与补充入口无法形成可用闭环。 |
| A-12 | `SOURCE_FIX` | 现有静态资料主要是本地演示数据，没有发现领养后端 request/cloud API 契约。 | 本轮可以完成可重复的本地演示状态机；真实生产数据接入需另有 API 合同和鉴权任务，不能宣称已接通生产链路。 |

## 3. 路由/状态覆盖矩阵

| 阶段 | Figma 节点 | 当前源码 | 审计状态 | 目标 |
| --- | --- | --- | --- | --- |
| 宠物详情/领养入口 | `62:29916`、`62:30091`、`62:30259` | `pages/adoption/petDetail.vue`、`PawPetDetailFigma` | PASS (LOCAL QA) | 入口提示关闭后进入选猫；页面截图核对通过 |
| 选猫 | `62:36699` | `pages/adoption/pickCats.vue`、`AdoptPickCatsSheet` | PASS (LOCAL QA) | Figma 独立素材、选择结果和禁用态通过 |
| 领养申请/长表单 | `62:31044`、`62:31158` | `pages/adoption/adoptApply.vue` | PASS (LOCAL QA) | 表单、媒体、选猫、recordId、成功跳转通过 |
| 申请成功 | `62:36497` | `pages/adoption/adoptApplySuccess.vue` | PASS (LOCAL QA) | 使用共享导航并进入对应领养详情 |
| 等待/审核/确认/拒绝/成功 | `62:31384`–`62:33198` | `PawAdoptionFlowFigma`、`pages/yard/adoptionAudit.vue` | PASS (LOCAL QA) | 主链路、拒绝、放弃状态持久化通过 |
| 我的领养/详情/申请内容 | `62:41525`、`62:41969`、`62:31825`、`62:31908` | `myAdoption`、`adoptionDetail`、`adoptionApplyContent` | PASS (LOCAL QA) | 原始 recordId、宠物数据和详情入口通过 |
| 确认领养/证实 | `62:35297`、`62:35393`、`62:37451` | `PawAdoptionEvidence`、`PawAdoptionProofForm` | PASS (LOCAL QA) | 证实表单、文字校验、保存、结果跳转通过 |
| 结果 | `62:36210`、`62:36267`、`62:36324`、`62:36381`、`62:36440`、`62:36497` | `pages/adoption/result.vue`、`PawFlowResult` | PASS (LOCAL QA) | `84`、`80` 结果 CTA 与 recordId 通过 |
| 奖励地址/投粮详情 | `62:36752`、`62:36782`、`62:36940`、`62:37087` | `submitOrder`、`feedingDetail` | PASS (LOCAL QA) | 地址预填、订单提交、奖励完成状态通过 |
| 额度/助力扩展 | `62:42711`、`62:42864`、`62:42949`、`62:38954` | `pages/adoption/extras.vue` | PASS (LOCAL QA) | 助力动物、额度明细、明细详情、额度不足关闭均通过 |

## 4. 实施任务

### P0：先打通本地可重复状态机

- [x] `ADOPT-001` 统一领养记录仓储：补充 `recordId`、角色、原始宠物/院主/申请内容/媒体/地址/证实数据；提供读取 demo + 本地记录、合法状态转换和刷新后的恢复。
- [x] `ADOPT-002` 修复选猫页：移除错误的 `figmaMode` 隐藏逻辑，使用 Figma 独立宠物素材；选中、禁用、最多 6 只、返回申请页均可用。
- [x] `ADOPT-003` 修复申请提交：保留选猫与媒体，提交前校验，写入真实记录 ID，并把 ID 传给成功页/我的领养。
- [x] `ADOPT-004` 修复“我的领养 → 详情 → 申请内容/领养信息”：不生成假 view ID，不覆盖原始宠物数据，缺失记录提供可回退的空态。
- [x] `ADOPT-005` 实现申请成功、结果 `80–85` 的 variant 路由：每个 CTA 使用 recordId 进入正确的订单/进度/详情/返回分支。
- [x] `ADOPT-006` 实现院主审核与申请人确认：同一记录可完成 `pending → pickup → owner_confirm → jury_confirm → reward` 或 `rejected/abandoned`，重复进入不破坏状态。
- [x] `ADOPT-007` 实现确认领养/证实：图片/视频选择、文本校验、提交保存和成功结果；“我也来证实”能够进入表单。
- [x] `ADOPT-008` 实现奖励地址：进入地址选择页、回填/保存地址、提交订单时校验并关联 recordId，再进入 `80` 领取结果。

### P1：按 Figma 还原页面与共享结构

- [x] `ADOPT-009` 领养页面迁移至 `PawPageNav`，删除页面级 native chrome 重绘和直接读取胶囊几何；底部操作改用 `PawFixedActionBar` 或语义化共享底栏。
- [x] `ADOPT-010` 清理领养范围的 `top-*.png`/顶部截图引用和页面截图式背景；保留独立 Figma 内容素材，改成 flex/流式卡片布局。
- [x] `ADOPT-011` 重做 `PawAdoptionFlowFigma` 的 frame 44–57：保留 24-unit 图标与 Figma 内容关系，按钮通过状态机工作，进度条不依赖 `nth-of-type`/屏幕绝对坐标。
- [x] `ADOPT-012` 重做 `PawAdoptionEvidence`/`PawAdoptionProofForm`：复用共享 overlay/form/action bar，修复确认页、证实列表和审核弹层的响应式布局。
- [x] `ADOPT-013` 完成额度/助力扩展页的真实交互：关闭不足弹窗、进入投喂/帮助页、额度明细回退和 recordId 上下文。

### P2：质量门禁

- [x] `ADOPT-014` 为关键节点补齐稳定 `qa-*` 选择器和状态断言，不使用深层 `nth-child`。
- [x] `ADOPT-015` 实现完成后逐项执行构建、`npm run check:native-ui`、必要时 `npm run verify:ui`，并做 Figma 节点视觉 review。
- [x] `ADOPT-016` 使用已打开的微信开发者工具完成本地全链路及拒绝/放弃分支验收，并记录 `BUILD/RUNTIME/CONSOLE/INTERACTION/GEOMETRY/VISUAL`；生产接口仍按第 5 节单独阻塞。

## 4.1 本轮审计：发布救助 → 证实救助

本轮不复用“领养主链路已通过”的结论，单独审计救助业务链路。设计节点已补录到 `docs/design/figma-map.yaml` 的 `rescue` 区域；实现以这些精确节点和本节任务为准。当前结论：**本地救助功能与唯一一次运行时全链路测试均已通过；生产接口和鉴权仍见第 5 节**。

### 4.1.1 路由与组件审计

| 阶段 | Figma 节点 | 当前入口/源码 | 当前结论 | 未通原因 |
| --- | --- | --- | --- | --- |
| 发布入口 | `62:29176` | `components/PawPublishSheetContent.vue` → `components/CustomTabber/index.vue:onPublishHelp` | PASS (RUNTIME) | 已通过稳定 QA 入口进入 `adoptApply?state=long&source=rescue`。 |
| 救助申请 | `62:31158` | `pages/adoption/adoptApply.vue?state=long&source=rescue` | PASS (RUNTIME) | 独立创建 rescue record，保存申请说明、金额、收款人、动物和媒体；协议未同意或金额为空时阻止提交。 |
| 救助评审列表（原救助基金池） | `62:34421` | `pages/yard/rescueReview.vue` | PASS (RUNTIME) | 救助基金池即救助评审列表，由 `utils/rescueStorage.js` 驱动统计和卡片，卡片传递 `rescueId`；使用流式/flex 卡片和本地 Figma 素材。 |
| 救助详情 | `62:33657` | `pages/feature/index.vue?mode=rescue-detail&rescueId=...` | PASS (RUNTIME) | 按 ID 加载救助记录，展示申请人、动物、证实概览，并提供证实列表和“我也来证实”入口。 |
| 证实列表 | `62:35393` | `pages/meMore/adoptionProofList.vue` + `PawAdoptionEvidence` | PASS (RUNTIME) | 通过 `source=rescue&rescueId=...` 读取 rescue proofList；共享用户跳转逻辑，文本和元信息保持无状态。 |
| 我也来证实 | `62:37451` | `pages/yard/adoptionAudit.vue?mode=proof&source=rescue` + `PawAdoptionProofForm` | PASS (RUNTIME) | 提交追加到同一 rescue record，协议和表单校验有效，完成后返回对应救助详情。 |

### 4.1.2 未通链路任务

- [x] `RESCUE-001` 建立统一本地救助 mock/storage：固定救助记录、状态、申请人、金额、收款信息、媒体、动物、证实人数和证实列表；提供按 `rescueId` 读取、创建、追加证实和刷新恢复 API，不复用 adoption 记录冒充救助记录。
- [x] `RESCUE-002` 修复发布入口：`流浪动物救助` 进入 Figma 长表单；提交前校验并创建 `rescueId`，提交成功后带 ID 进入救助评审列表，保留共享 `PawPageNav` 与底部操作栏。
- [x] `RESCUE-003` 重做救助评审列表：使用统一救助 mock 驱动统计、状态卡片、媒体和状态标签；卡片点击传递 `rescueId`，采用 flex/流式布局并对齐 `62:34421`，不使用页面截图切图。
- [x] `RESCUE-004` 重做救助详情：加载对应 `rescueId`，补齐救助申请主体、动物、证实概览及“查看全部/我也来证实”入口；对齐 `62:33657`，复用共享身份、标签、按钮和导航组件。
- [x] `RESCUE-005` 扩展证实列表组件为救助来源：列表使用 `source=rescue` 和 `rescueId` 读取统一 mock，展示证实人和剩余证实内容；仅用户名称/头像可跳转用户，证实文本和元信息保持无状态。
- [x] `RESCUE-006` 扩展证实表单为救助来源：保留表单说明、协议校验和提交；提交追加到救助记录并回到对应救助详情，不能误更新 adoption 状态，样式对齐 `62:37451`。
- [x] `RESCUE-007` 补齐 rescue 路由上下文和权限：普通用户可浏览救助和证实列表，救助来源不进入领养审核修改分支；保留 `rescueId`，并提供 `canManageRescue` 作为未来发起人/管理员修改操作的权限 seam。
- [x] `RESCUE-008` 完成救助链路 UI review：逐页核对 `62:31158`、`62:34421`、`62:33657`、`62:35393`、`62:37451`，优先 flex/流式布局，使用 Figma 独立源素材，不引入完整画板或局部截图。
- [x] `RESCUE-009` 所有救助任务完成后，使用已打开的微信开发者工具只执行一次完整链路测试：发布入口 → 救助申请 → 基金池 → 救助详情 → 证实列表 → 我也来证实 → 提交 → 返回详情，并记录 `BUILD/RUNTIME/CONSOLE/INTERACTION/GEOMETRY/VISUAL`。

## 5. 生产接口前置项

当前仓库未发现领养 request/cloud API 及数据合同。因此本任务清单中的“打通”先以本地持久化状态机为验收对象；要宣称生产链路完成，还需要补充：

- 领养记录、审核、拒绝、确认、证实、奖励订单的接口契约和错误码；
- 登录用户/院主/评审团角色与权限规则；
- 媒体上传策略、内容审核、地址脱敏与订单关联规则；
- 真实接口接入后的 network 验收用例。

## 6. 2026-09-03 全链路验收记录

- `BUILD`：`npm run verify:ui` 通过；主包 `1676.6KB`，低于 `2048KB` 硬限制，但仍高于 `1.5MB` 质量建议。
- `RUNTIME`：复用已打开的微信开发者工具窗口，运行目录为 `unpackage/dist/dev/mp-weixin`；清理编译缓存后重新刷新，组件解析错误消失。
- `CONSOLE`：领养相关页面未发现运行时 error；保留既有 Sass 弃用提示及非领养范围的基础库兼容提示。
- `INTERACTION`：入口、选猫、申请、成功、我的领养、详情、院主审核、申请人确认、证实、评审、奖励地址、结果均已验证；`rejected`、`abandoned`、助力动物、额度明细、明细详情和额度不足关闭分支也已验证并正确回退。
- `GEOMETRY`：领养范围页面统一使用 `PawPageNav`、`PawFixedActionBar`/流式卡片布局；未新增业务侧 native chrome 重绘。
- `VISUAL`：已核对选猫、申请成功、领养进度、审核、证实、订单等截图与 Figma 结构；QA 截图保存在 `.artifacts/`，未纳入提交。
- 验收使用的是本地 QA/demo 记录与本地 storage 状态机；`chooseMedia` 的真实系统媒体选择器和生产 network 链路仍需真实接口/账号环境验收。

## 7. 2026-09-03 救助发布 → 证实最终验收

- `BUILD`：`npm run build:mp-weixin` 通过；`npm run check:native-ui` 通过；`npm run verify:ui` 通过。主包低于 `2048KB` 硬限制，但仍高于 `1.5MB` 质量建议。
- `RUNTIME`：复用已打开且指向 `unpackage/dist/dev/mp-weixin` 的微信开发者工具窗口，仅执行一次完整链路：发布入口 → 救助申请 → 救助评审列表 → 救助详情 → 证实列表 → 我也来证实 → 提交 → 返回救助详情。
- `CONSOLE`：本次链路未发现运行时 error；保留既有 Sass 弃用提示及基础库兼容提示。
- `INTERACTION`：救助申请协议勾选和字段校验生效；提交生成独立 `rescueId`；基金池卡片按 ID 进入详情；证实列表读取同一记录；证实表单提交后证实人数和列表更新，并返回对应救助详情。
- `GEOMETRY`：申请页滚动内容实际测得 `390×753`；底部提交操作栏保持安全区布局；救助页面未新增业务侧 native chrome 重绘。
- `VISUAL`：已按 `figma-map.yaml` 中的精确节点 `62:31158`、`62:34421`、`62:33657`、`62:35393`、`62:37451` 做结构 review；使用本地 Figma 独立素材和 flex/流式布局。运行时截图保存在 `.artifacts/rescue-runtime-proof.png`，未纳入提交。
- 本次验收对象为本地 QA/demo storage 状态机；真实生产接口、登录鉴权、媒体上传和真实支付不在本地验收范围，详见第 5 节。
